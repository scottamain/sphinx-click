---
version: 2
python:
  install:
    - method: pip
      path: .
sphinx:
  builder: "dirhtml"
  configuration: "docs/conf.py"
build:
  os: "ubuntu-24.04"
  tools:
    python: "3.13"
  jobs:
    post_checkout:
      - git fetch --unshallow
