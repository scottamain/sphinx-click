---
version: 2
python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
sphinx:
  builder: "dirhtml"
  configuration: "docs/conf.py"
build:
  os: "ubuntu-24.04"
  tools:
    python: "3.13"
  jobs:
    post_checkout:
      - git fetch --unshallow
